{
    "name": "pakmsh",
    "build": {
      "dockerfile": "../docker/Dockerfile",
      "target": "pakmsh_user" // Specify the target stage
    },
    "settings": {
      "terminal.integrated.shell.linux": "/bin/bash"
    },
    "postCreateCommand": "apt-get update",
    "remoteUser": "root"
  }
  